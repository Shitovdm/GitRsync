package Helpers

func Clone(repositoryPath string, projectName string) {
	//rootDirPath := Configuration.BuildPlatformPath(fmt.Sprintf("/projects/%s", projectName))
	//Exec(fmt.Sprintf("git clone %s %s", repositoryPath, rootDirPath))
}